package com.petsistemi.domain;

/** Reaction kinds supported by the per-pet {@code reactions:} section. */
public enum PetReactionType {
    OWNER_DAMAGE,
    LEVEL_UP,
    REST_START,
    REST_END
}
