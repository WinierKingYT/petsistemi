package com.petsistemi.domain;

/** Fixed anchor slots relative to the owner's facing direction (no pathfinding). */
public enum PetAnchorPosition {
    BEHIND_RIGHT,
    BEHIND_LEFT,
    FRONT,
    ABOVE_HEAD,
    RIGHT_SHOULDER,
    LEFT_SHOULDER,
    WAIST,
    BELOW
}
