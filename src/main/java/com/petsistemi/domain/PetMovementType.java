package com.petsistemi.domain;

/** How a pet moves relative to its owner. */
public enum PetMovementType {
    GROUND_FOLLOW,
    FLYING_FOLLOW,
    HOVER,
    ORBIT,
    TRAIL,
    FORMATION,
    SHOULDER,
    ANCHORED,
    STATIC_NEAR_OWNER,
    TELEPORT_ONLY,
    ECHO,
    SHADOW_TRAIL,
    ROAM_NEAR_OWNER,
    MIRROR,
    SWARM_CLOUD
}
